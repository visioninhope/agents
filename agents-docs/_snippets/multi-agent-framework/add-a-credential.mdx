Make sure your Visual Builder is running (if you haven't set it up yet, see [Quick Start](/getting-started/quick-start)). Navigate to the Credentials tab in the left sidebar and click "New credential".

Select "Bearer Authentication" and click "Create Credential". You'll then be prompted to enter your API key for the MCP server.
